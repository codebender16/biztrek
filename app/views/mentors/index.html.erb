<h1>Welcome back, <em><%= current_user.first_name %></em></h1>

<div class="row justify-content-around">
  <% @courses.each do |course| %>
    <%= render 'shared/card', course: course %>
    <% end %>
  
</div>