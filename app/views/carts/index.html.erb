<h1>Cart</h1>

<ul>
  <% @cart.each do |course| %>
    <li><%= course.title %> | <%= number_to_currency(course.price) %></li>
  <% end %>
</ul>

<%= link_to "Pay Now 💵", "", id: "stripe" %>

<%= javascript_pack_tag "stripe" %>